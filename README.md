## doxygenTools
Tools for working with doxygen files.

This can be used as a plugin to codeTools.


### fdox
A simple tool to make doxygen generation for a directory as simple and painless as possible.

It's as simple as saying:
`fdox ./`
and it will find source directories, create Doxyfile configuration, add doxygen assets to gitignore, and generate doxygen docs